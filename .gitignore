# OS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
# Cursor — local-only (public repo; do not commit rules)
.cursor/
*.swp
*.swo
*~

# Dependencies (if any)
node_modules/

# Local / temp
*.log
.env
.env.local
*.zip

# Optional: one-off audits (uncomment if you don’t want to track them)
# *-AUDIT.md

# Survey / report data (generated or local, not part of docs)
survey-results-full.json
survey-results-for-analysis.csv
survey-responses-by-question.csv
survey-users-list.json
reseller-survey-users-report.json
fetch_survey_results.py
